<div>
  <div class="row" style="color: red;">
      <% if @user.errors.any? %>
          <h2 class="error-message"><%= pluralize(@user.errors.count, "error") %> prohibited this book from being saved:</h2>
          <ul class="error-text">
            <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
          </ul>
      <% end %>
  </div>
  <div class="row" >
  <h3>User info</h3>
  <div class="col-xs-11">
  <%= form_for (@user) do |f| %>
    <div>
      <label>Name</label>
      <%= f.text_field :name %>
    </div>
    <div>
        <label>Image</label>
        <%= f.attachment_field :profile_image, placeholder: "プロフィール画像", class: "user-edit-form" %>
    </div>
    <div>
    <label>Introduction</label>
    <%= f.text_area :introduction %>
    </div>
    <div>
      <%= f.submit "Update User" %>
    </div>
  <% end %>
  </div>
  <div class="col-xs-1">
    <%= link_to "登録情報", edit_user_registration_path(current_user) %>
  </div>
  </div>
</div>