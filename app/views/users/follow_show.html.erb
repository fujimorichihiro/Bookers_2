<div class="row">
	<div>
      <h2>Follow Usres</h2>
	    <table class="table">
	    	<thead>
	    	    <tr>
	    			<th>image</th>
	    			<th>name</th>
	    			<th></th>
	    			<th></th>
	    			<th></th>
	    		</tr>
	    	</thead>
	    		<tbody>
	    			<% @user.following.each do |follow| %>
	    			<tr>
	    				<td><%= attachment_image_tag follow, :profile_image, :fill, 40, 40, class: "img-round",fallback: "no_image.jpg" %></td>
	    				<td>
	    					<%= link_to user_path(follow) do %>
	    					  <%= follow.name %>
	    					<% end %>
	    				</td>
	    				<td>フォロー数: <%= follow.following.count %></td>
	    				<td>フォロワー数: <%= follow.followers.count %></td>
	    				<td><%= link_to "Show", user_path(followed) %></td>
	    			</tr>
	    			<% end %>
	    	    </tbody>
	    </table>
	</div>
</div>
